d"امر  تثبيت"
code"sudo  apt install apache2"

d"امر اعدادات الاداه دي"

code"sudo nano /etc/apache2/ports.conf"

d"امر  مسار  الويب تقريبا هوا بيكون افتراضي لاي  اداه زي  apache2 بنسبا لتوزيعه لينكس  المهم تقدر تنقل اي فايل  html,php او  او اكتر  بنسبا   لدول وبس كدا" 
code"/var/www/html" 

d"امر  تشغيل اداه apache2"

code"sudo service apache2 start"

d"امر اعاده   تحميل الاداه او لود apache2"
code"sudo service apache2 reload"

d"امر  توقف  سرفر لاداه apache2"

code"sudo service apache2 stop"

d"امر اللغاء  تثبيت او حذف  الاداه"

code"sudo apt  remove apache2"
